version: '3'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:     
      - db
    
  db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: password@drc
      MYSQL_DATABASE: drc_challenge