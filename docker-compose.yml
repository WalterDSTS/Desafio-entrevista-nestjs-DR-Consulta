version: '3'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:     
      - db
    
  db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: drc_test
      MYSQL_USERNAME: dev
      MYSQL_PASSWORD: password